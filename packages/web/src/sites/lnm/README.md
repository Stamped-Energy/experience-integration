LNM Faridabad **site pack** (L6 catalog + fixture assets). Not the platform default plant.

Set `STAMPED_DEFAULT_PLANT_ID` / `NEXT_PUBLIC_STAMPED_DEFAULT_PLANT_ID` to `plant_lnm_faridabad_1` when this site should be the session default.

See workspace `docs/PRODUCT_LAYERS.md`.
