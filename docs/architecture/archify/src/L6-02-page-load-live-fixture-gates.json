{
  "schema_version": 1,
  "diagram_type": "sequence",
  "meta": {
    "title": "Page load — Live vs Preview gates",
    "output": "L6-02-page-load-live-fixture-gates.html",
    "quality_profile": "showcase",
    "locale": "en",
    "visual_preset": "classic",
    "animation": "trace",
    "viewBox": [980, 520],
    "legend": { "mode": "hidden" },
    "column_fit": "spread",
    "views": [
      {
        "id": "open-page",
        "label": "Open a screen",
        "focus": ["browser", "web", "bff"],
        "note": "User navigates; Next.js loads route data via BFF session cookies."
      },
      {
        "id": "probe-gates",
        "label": "Probe upstream gates",
        "focus": ["web", "bff", "gates"],
        "note": "AppShell polls GET /api/meta/upstreams; BFF reads USE_FIXTURES and L2/L5/L4 live flags."
      },
      {
        "id": "badge",
        "label": "Live vs Preview pill",
        "focus": ["bff", "gates", "source", "pill"],
        "note": "Live = BFF reached configured L2/L5 (and L4 when L4_LIVE); Preview = fixtures or upstream down. Not a customer plant."
      }
    ]
  },
  "participants": [
    { "id": "browser", "type": "external", "label": "Browser", "sublabel": "BUILT · operator" },
    { "id": "web", "type": "frontend", "label": "Next.js web", "sublabel": "BUILT · :3000" },
    { "id": "bff", "type": "backend", "label": "Fastify BFF", "sublabel": "BUILT · :3001" },
    { "id": "gates", "type": "security", "label": "Live gates", "sublabel": "BUILT · env defaults" },
    { "id": "source", "type": "cloud", "label": "Data source", "sublabel": "PARTIAL · live|fixture" },
    { "id": "pill", "type": "frontend", "label": "Data pill", "sublabel": "BUILT · AppTopbar" }
  ],
  "messages": [
    { "from": "browser", "to": "web", "y": 160, "label": "GET route (e.g. /alarms)", "variant": "emphasis" },
    { "from": "web", "to": "bff", "y": 188, "label": "GET /api/* + session", "variant": "emphasis" },
    { "from": "bff", "to": "gates", "y": 216, "label": "USE_FIXTURES · L*_LIVE", "variant": "default" },
    { "from": "bff", "to": "source", "y": 244, "label": "live HTTP or fixtures", "variant": "default" },
    { "from": "source", "to": "bff", "y": 272, "label": "JSON payload", "variant": "return" },
    { "from": "web", "to": "bff", "y": 300, "label": "GET /api/meta/upstreams", "variant": "emphasis" },
    { "from": "bff", "to": "web", "y": 328, "label": "demoMode + l2/l5/l4 status", "variant": "return" },
    { "from": "web", "to": "pill", "y": 356, "label": "connectionPillLabel", "variant": "default" },
    { "from": "pill", "to": "browser", "y": 384, "label": "Live or Preview label", "variant": "return" }
  ],
  "cards": [
    {
      "dot": "cyan",
      "title": "Status & sources",
      "items": [
        "experience-integration@82df09ee · config.ts · meta/upstreams.ts:116-118",
        "demoMode when configured L2 or L5 live probe fails (L4 not in demoMode)",
        "Page data: USE_FIXTURES forces fixture upstreams (index.ts:34-52)"
      ]
    },
    {
      "dot": "emerald",
      "title": "Gate truth table",
      "items": [
        "Live = BFF reached L2/L5 (L4 if L4_LIVE); Preview = fixtures or upstream down",
        "Live does NOT mean a customer plant — no integration is live at any plant",
        "UI copy Live plant connected (client-copy.ts) can mislead newcomers"
      ]
    }
  ]
}
